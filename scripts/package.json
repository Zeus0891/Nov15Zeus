{
  "name": "@beesmart/rls-generator",
  "version": "1.0.0",
  "description": "RLS Policy Generator for BeeSmart ERP",
  "scripts": {
    "generate:rls": "npx tsx scripts/generate-rls-policies.ts",
    "generate:rls:dry": "npx tsx scripts/generate-rls-policies.ts --dry-run",
    "validate:schemas": "npx tsx scripts/validate-prisma-schemas.ts"
  },
  "devDependencies": {
    "tsx": "^4.6.2",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
